<div class="brand-right">
    <nav>
        <ul>
            <li><a href="{{ url_for('home') }}" class="px-3 {% if route_name=="home" %} active {% endif %}"><i class="fa fa-home"></i></a></li>
            {% if not auth.check %}<li><a href="{{ url_for('auth.sign.in') }}">Masuk</a></li>{% endif %}
        </ul>
    </nav>
</div>